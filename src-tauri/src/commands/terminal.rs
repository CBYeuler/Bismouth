// Terminal commands — add #[tauri::command] fns here as the terminal feature grows.
//
// Example stubs:
//
// #[tauri::command]
// pub fn spawn_terminal(cwd: String) -> Result<u32, AppError> {
//     terminal_service::spawn_shell(&cwd)
// }
 